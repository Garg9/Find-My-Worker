<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add Category and Services</title>
</head>
<body>
  <h1>Add a New Category with Services</h1>
  <form action="/api/admin/categories" method="POST">
    <label for="name">Category Name:</label>
    <input type="text" name="name" required>

    <h3>Services</h3>
    <div id="service-fields">
      <div>
        <input type="text" name="services[0][name]" placeholder="Service Name" required>
        <input type="number" name="services[0][price]" placeholder="Price" required>
      </div>
    </div>

    <button type="button" onclick="addService()">Add More Services</button>
    <button type="submit">Submit</button>
  </form>

  <script>
    let index = 1;
    function addService() {
      const container = document.getElementById('service-fields');
      const div = document.createElement('div');
      div.innerHTML = `
        <input type="text" name="services[${index}][name]" placeholder="Service Name" required>
        <input type="number" name="services[${index}][price]" placeholder="Price" required>
      `;
      container.appendChild(div);
      index++;
    }
  </script>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Add Category and Services - Find My Worker</title>
    <link href="/styles.css" rel="stylesheet" type="text/css" />
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
        }
        .nav-bar {
            background-color: #333;
            padding: 10px 0;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }
        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
        }
        .logotext {
            color: white;
            text-decoration: none;
            font-size: 24px;
            font-weight: bold;
        }
        .container {
            max-width: 600px;
            margin: 40px auto;
            background-color: white;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }
        h2 {
            text-align: center;
            color: #333;
        }
        form {
            display: flex;
            flex-direction: column;
        }
        label {
            margin-top: 15px;
            font-weight: bold;
            color: #555;
        }
        input, textarea, select {
            margin-top: 5px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }
        button {
            margin-top: 25px;
            padding: 12px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #0056b3;
        }
        .back-link {
            text-align: center;
            margin-top: 20px;
        }
        .back-link a {
            text-decoration: none;
            color: #007bff;
        }
    </style>
</head>
<body>
    <div class="nav-bar">
        <div class="nav-container">
            <a href="/api/admin/home" class="logotext">Find My Worker</a>
        </div>
    </div>

    <div class="container">
        <h2>Add a New Category with Services</h2>
        <form action="/api/admin/categories" method="POST">
            <label for="name">Category Name</label>
            <input type="text" name="name" id="name" placeholder="Enter category name" required>

            <h3>Services</h3>
            <div id="service-fields">
                <div class="form-group">
                    <input type="text" name="services[0][name]" placeholder="Service Name" required>
                    <input type="number" name="services[0][price]" placeholder="Price" required>
                </div>
            </div>

            <button type="button" onclick="addService()">Add More Services</button>
            <button type="submit">Submit</button>
        </form>

        <div class="back-link">
            <a href="/api/admin/pairing">← Back</a>
        </div>
    </div>

    <script>
        let index = 1;
        function addService() {
            const container = document.getElementById('service-fields');
            const div = document.createElement('div');
            div.classList.add('form-group');
            div.innerHTML = `
                <input type="text" name="services[${index}][name]" placeholder="Service Name" required>
                <input type="number" name="services[${index}][price]" placeholder="Price" required>
            `;
            container.appendChild(div);
            index++;
        }
    </script>
</body>
</html>
